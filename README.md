# tiddlywiki-context-menu-plugin

This plugin add context menus to tiddlers.

## Demo

See https://context-menu-plugin.tiddlyhost.com/

## Build

Build the documentation wiki with `make`.

```
make install
make
```

Then open `index.html` in your browser.
